{
  "name": "cyanluna.portfolio",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "test": "vitest run",
    "test:watch": "vitest",
    "optimize:images": "node scripts/optimize-images.mjs",
    "generate:placeholders": "node scripts/generate-placeholders.mjs",
    "seed:unahouse": "npx tsx scripts/capture/seeds/unahouse-finance.ts --reset",
    "capture:unahouse": "npx tsx scripts/capture/scenarios/unahouse-finance.ts",
    "capture:today-bike": "npx tsx scripts/capture/scenarios/today-bike.ts",
    "capture:ai-cycling-coach": "npx tsx scripts/capture/scenarios/ai-cycling-coach.ts",
    "capture:kanban": "npx tsx scripts/capture/scenarios/kanban-pipeline.ts",
    "capture:cpet": "npx tsx scripts/capture/scenarios/cpet-platform.ts",
    "capture:ride-analytics": "npx tsx scripts/capture/scenarios/ride-analytics.ts",
    "capture:assist-11th": "npx tsx scripts/capture/scenarios/assist-11th.ts",
    "capture:assist-hub": "npx tsx scripts/capture/scenarios/assist-hub.ts",
    "capture:equipment-gateway": "npx tsx scripts/capture/scenarios/equipment-gateway.ts",
    "capture:resource-board": "npx tsx scripts/capture/scenarios/resource-board.ts",
    "capture:smart-factory-qc": "npx tsx scripts/capture/scenarios/smart-factory-qc.ts",
    "capture:javis": "npx tsx scripts/capture/scenarios/javis.ts",
    "capture:code-review": "npx tsx scripts/capture/scenarios/code-review-suite.ts",
    "capture:all": "pnpm seed:unahouse && pnpm capture:unahouse && pnpm capture:today-bike && pnpm capture:ai-cycling-coach && pnpm capture:kanban && pnpm capture:cpet && pnpm capture:ride-analytics && pnpm capture:assist-11th && pnpm capture:assist-hub && pnpm capture:equipment-gateway && pnpm capture:resource-board && pnpm capture:smart-factory-qc && pnpm capture:javis && pnpm capture:code-review && pnpm optimize:images",
    "demo:cpet:sync": "bash scripts/sync-cpet-report.sh",
    "demo:cpet:deploy": "bash scripts/deploy-demo-static.sh"
  },
  "dependencies": {
    "next": "16.1.6",
    "react": "19.2.3",
    "react-dom": "19.2.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@vitest/coverage-v8": "^4.1.0",
    "eslint": "^9",
    "eslint-config-next": "16.1.6",
    "playwright": "^1.58.2",
    "sharp": "^0.34.5",
    "tailwindcss": "^4",
    "typescript": "^5",
    "vite-tsconfig-paths": "^6.1.1",
    "vitest": "^4.1.0"
  }
}
